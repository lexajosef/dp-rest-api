const User = require('../models/user');
const Joi = require('joi');

class usersController {

  static async createUser(req, res) {
    // TODO: validate and create new user
  }
}

module.exports = usersController;
